{
  "name": "@lbrx/shared-core",
  "version": "0.0.0",
  "private": true,
  "description": "Shared types, constants and utilities reused across LBRX Obsidian plugins. Not published to npm; consumed via workspace protocol and bundled into each plugin at build time.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
