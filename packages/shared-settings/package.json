{
  "name": "@lbrx/shared-settings",
  "version": "0.0.0",
  "private": true,
  "description": "Read/write helpers for the optional cross-plugin shared data file (lbrx-shared.json), used for loosely-coupled runtime data exchange between LBRX plugins.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@lbrx/shared-core": "workspace:*"
  },
  "devDependencies": {
    "obsidian": "latest",
    "typescript": "^5.5.0"
  }
}
