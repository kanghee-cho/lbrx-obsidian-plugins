{
  "name": "@lbrx/shared-ui",
  "version": "0.0.0",
  "private": true,
  "description": "Reusable Obsidian UI building blocks (base setting-tab class, common modals) shared across LBRX plugins. Bundled at build time, not published.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@lbrx/shared-core": "workspace:*"
  },
  "devDependencies": {
    "obsidian": "latest",
    "typescript": "^5.5.0"
  }
}
